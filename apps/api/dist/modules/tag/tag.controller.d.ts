import { TagService } from './tag.service';
import { CreateTagDto } from './dto';
export declare class TagController {
    private readonly tagService;
    constructor(tagService: TagService);
    findAll(req: any): Promise<{
        tags: {
            id: string;
            name: string;
            color: string;
            contentCount: number;
        }[];
    }>;
    create(req: any, dto: CreateTagDto): Promise<{
        id: string;
        name: string;
        createdAt: Date;
        userId: string;
        color: string;
        autoGenerated: boolean;
    }>;
}
export declare class ContentTagController {
    private readonly tagService;
    constructor(tagService: TagService);
    addTag(req: any, contentId: string, dto: {
        tagId: string;
    }): Promise<{
        success: boolean;
    }>;
    removeTag(req: any, contentId: string, tagId: string): Promise<{
        success: boolean;
    }>;
}
