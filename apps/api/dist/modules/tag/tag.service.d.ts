import { PrismaService } from '../../prisma/prisma.service';
export declare class TagService {
    private prisma;
    constructor(prisma: PrismaService);
    findAll(userId: string): Promise<{
        id: string;
        name: string;
        color: string;
        contentCount: number;
    }[]>;
    create(userId: string, name: string, color?: string): Promise<{
        id: string;
        name: string;
        createdAt: Date;
        userId: string;
        color: string;
        autoGenerated: boolean;
    }>;
    addTagToContent(contentId: string, tagId: string): Promise<{
        success: boolean;
    }>;
    removeTagFromContent(contentId: string, tagId: string): Promise<{
        success: boolean;
    }>;
}
